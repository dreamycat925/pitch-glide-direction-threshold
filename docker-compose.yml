services:
  pitch-glide:
    build: .
    container_name: pitch-glide-direction-threshold
    ports:
      # Bind only to localhost for safety (not exposed to LAN)
      - "127.0.0.1:30000:8501"
    restart: unless-stopped
    environment:
      TZ: "Asia/Tokyo"
